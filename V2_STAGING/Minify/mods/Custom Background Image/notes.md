<!-- lang:en -->

Uses a static custom image as your Dota 2 background.

- Click `Select Background Image` in settings, or place `background.png`, `background.jpg`, or `background.webp` in `Minify/config/`.
- If using `.jpg` or `.webp`, ImageMagick (`magick`) is required to automatically convert it to `.png`.
