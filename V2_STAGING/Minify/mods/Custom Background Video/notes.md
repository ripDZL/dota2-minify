<!-- lang:en -->

Plays a custom video as your Dota 2 dashboard background.

- Click `Select Background Video` in settings, or place `background.webm` or `background.mp4` in `Minify/config/`.
- If using `.mp4`, FFmpeg is required to automatically convert it to VP9 WebM format for Dota 2 Panorama compatibility.
